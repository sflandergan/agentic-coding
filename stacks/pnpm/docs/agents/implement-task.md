## pnpm Stack Additions

- Use `pnpm test` for unit tests when present.
- Use `pnpm lint` and `pnpm typecheck` for code quality checks when present.
- Include additional project-documented pnpm checks when the task scope requires them.
