## Maven Stack Additions

- Use `mvn test` for unit tests.
- Use `mvn verify` for full verification when the task scope requires it.
- Use `mvn clean verify` only when the target project explicitly documents or requests a clean rebuild.
